/* exported head */
function head(array) {
  var first = array[0];
  return first;
}

// takes the first index value of the array
// find the first index by storing the array[0] in a variable
// return the variable
