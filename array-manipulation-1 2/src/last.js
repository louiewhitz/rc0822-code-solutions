/* exported last */
function last(array) {
  var lastValue = array[array.length - 1];

  return lastValue;
}

// find the last index value in the array and store it in a variable
//  use array[array.length - 1] - will return last value
