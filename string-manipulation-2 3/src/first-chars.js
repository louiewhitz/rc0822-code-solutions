/* exported firstChars */
function firstChars(length, string) {
  var newStr = string.slice(0, length);
  return newStr;
}
// the length is the input number at which the new string should be split
// use the slice method and slice it at the character
