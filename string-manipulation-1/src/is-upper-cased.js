/* exported isUpperCased */
function isUpperCased(word) {
  var uppercase = word.toUpperCase();

  if (word === uppercase) {
    return true;
  }
  return false;
}

// check the word and see if it is uppercase
// use a boolean to see if it is true or false
