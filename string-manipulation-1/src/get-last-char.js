/* exported getLastChar */
function getLastChar(string) {
  var last = string[string.length - 1];
  return last;
}

// to get the last character, you must use the length of the string -1 as the index;
